const { expect } = require('chai');
const parse = require('../parser');
describe('Paywall Reader Tests', () => {
    it ('should check New York Times validity', () => {
        expect(true).to.be.true;
    });
});